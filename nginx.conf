server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: F5 на вложенных путях после strip префикса прокси
  location / {
    try_files $uri $uri/ /index.html;
  }
}
